/**
 * Expo Go cannot reach a server bound to `localhost` on the development
 * computer. Start the API on `0.0.0.0`, then use the computer's LAN IP in the
 * mobile app (for example, `http://192.168.1.20:3000`), and keep both devices
 * on the same Wi-Fi network. Also allow the API port through the firewall.
 */
 
